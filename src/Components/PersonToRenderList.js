import React from "react";
function PersonToRenderList(props){
    return (
        <h2> rendering from sep js , hello {props.name} with ID {props.id} of {props.age} years old</h2>
)
}

export default PersonToRenderList
